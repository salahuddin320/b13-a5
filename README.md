    - 1️⃣ What is the difference between var, let, and const?
    var old version for variable declaration and it is a hoisting.
    let use when we use variable value.
    const use when we use constant value

    - 2️⃣ What is the spread operator (...)?
    One array show inside the another array using the spread operator.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    map() is a method where it is use for take value one by one as like for loop.
    filter() is a method where it is filtering the value.
    forEach() it is a method where we collecting element into a array.

    - 4️⃣ What is an arrow function?
    Arrow function which is property of es6, it is doing sh0rt the code very popular function for es6.

    - 5️⃣ What are template literals?
    Template literals allow for embedded expression. Backtick use (``) for showing all expression.